# 自動投票 仕様書（keirin → WINTICKET / Windows）

2026-09-21 作成。**これは仕様書であって、動くプログラムではありません。**
Windows のご自身のPCで Claude Code などに実装させるときに、この文書ごと読ませてください。

競艇側（`honda1986/v24` の `auto_bet/`）と同じ作りにしてあります。
**インフラ部分（`browser.py` / `jst.py` / `config.py` / `bet_store.py` / `betlog.py`）は
ほぼそのまま流用できます。** 新しく書くのは3つだけです。

---

## 0. 前提 — 先に確認すること

**WINTICKET の利用規約を、ご自身で読んで判断してください。**
自動操作の扱いが書かれているはずです。競艇側の `SPEC.md` と同じく、
設定の `i_have_read_the_terms` を `true` にするまで実際の投票はしない作りにします。

この文書を書いた Claude は **winticket.jp に到達できない環境にいた**ため、
規約もページ構造も確認できていません。**セレクタはご自身のPCで採取が必要です。**

### もう一つ、先に読むこと

現在の回収率は **104.7%（95%区間 96.7〜113.7%）** で、**100%をまたいでいます。**
自動化するということは、**まだ確かめている途中のルールで機械的に賭け続ける**
ということです。止めやすい作り（1日の上限・停止ファイル・dry既定）は仕様に
入れてありますが、運用の判断はご自身でお願いします。

---

## 1. 全体の形

```
GitHub Actions (keirin)            ご自身の Windows PC
  fetch.js が1日3回 予想             keirin_bet が2分おきに取りに行く
        ↓                                    ↓
  races.json を push ────────→  plan.hot のレースを選ぶ
  (main ブランチ)                             ↓
                                    Chrome を操作して WINTICKET で投票
                                             ↓
                                    bet_done.json に即記録
```

**予想側（GitHub Actions）はいっさい触らないこと。** `races.json` を読むだけ。

---

## 2. 入力：races.json の読み方

```
https://raw.githubusercontent.com/honda1986/keirin/main/races.json?t=<毎回変わる数>
```

### ★キャッシュに必ず引っかかる

`raw.githubusercontent.com` は **最大5分ほどキャッシュされます。**
`Cache-Control: no-cache` ヘッダだけでは足りません（CDN 側に効かない）。
**URL の末尾に毎回変わるクエリを付けること。**

```python
url + "?t=" + str(int(time.time()))
```

### 2-1. 構造

```json
{
  "updatedAt": "2026-09-21T08:45:27.932Z",
  "count": 72,
  "races": [ ... ]
}
```

**`races.json` は当日ぶんだけで、毎回まるごと置き換わります。**
競艇の `history.json` と違って日付ごとの配列ではありません。

### 2-2. races の1件（投票に使うものだけ）

```json
{
  "key": "富山_4R",
  "place": "富山",
  "raceNo": "4R",
  "startTime": "10:31",          ← ★発走予定。締切ではない（§3）
  "date": "2026年09月21日",
  "plan": {
    "hot": true,                 ← ★これが true のレースだけ買う
    "cars": 9,
    "trio": [5, 3, 6],           ← 隊列順。買う3車
    "ticket": "3=5=6",           ← ★昇順。三連複1点。これを買う
    "rankSum": 12,
    "needOdds": false,           ← ★§4。false のときだけ買う
    "bandLo": null, "bandHi": null,
    "roi": 107.3, "hitRate": 17.8
  }
}
```

**投票に必要なのは `place` / `raceNo` / `startTime` / `plan` だけ。** 他は無視してよい。

`plan` が `null` のことがあります（本命ラインが3人未満など）。`plan and plan.get("hot")`
の形で扱ってください。

### 2-3. ★投票の時点で買い目を作り直さないこと

`plan.ticket` をそのまま買ってください。
**`riders` や `lines` から自分で計算し直さないでください。**

理由: 判定ロジック `f3PlanFrom` は `engine.js` と `index.html` の2箇所にあり、
既に「片方だけ直して事故る」形になっています。2026-08-28 の取得元移行では
`results.js` が取り残されて **27日間、結果が1件も記録されませんでした。**
3つ目のコピーを作らないために、`plan` を `fetch.js` が書き出すようにしてあります。

---

## 3. 締切時刻 ★ここが競艇版と一番違う

**`races.json` は発走予定時刻（`startTime`）しか持っていません。締切時刻を持っていません。**

競艇版の `close` は「ネット締切（本場締切の3分前）」が既に入っていましたが、
keirin 側にはそれがありません。

### どうするか

**WINTICKET のレースページに表示されている締切を読むこと。** それが最も確かです。

`startTime` は「どのレースを見に行くか」の当たりを付けるためだけに使い、
**買う/買わないの最終判断はサイト上の締切で行ってください。**

`startTime` から締切を引き算で推定するのは避けてください。
競輪のネット投票の締切が発走の何分前かは、この文書を書いた時点で**未確認**です。
場やグレードで違う可能性もあります。

### 安全側に倒す

- 締切までの分数が大きく負（例 −60分より小さい）なら「もう過ぎた」として黙って飛ばす
- 締切が読めなかったら**買わない**。推測で買わない
- **PC の時計とタイムゾーンが JST であること**

---

## 4. どのレースを買うか ★第2段階では9車立てだけ

```python
if not plan or not plan.get("hot"):
    skip
if plan.get("needOdds"):
    skip            # ← 第2段階では買わない
```

| | `needOdds` | オッズ確認 | 第2段階 |
|---|---|---|---|
| 9車立て(8車以上)・単騎1人以下 | `false` | 不要 | **買う** |
| 7車立て・順位合計12以上 | `true` | 必須 | **買わない** |

### なぜ7車立てを外すか

7車立ての条件は「三連複オッズが 4〜15倍」で、**帯を外すと実測87.7%**
（帯内は102.3%）。確認せず全部買うと 96.1% でマイナスになります。

問題は、**その4〜15倍という帯を確定オッズで測っている**ことです。
`odds.js` はレース後に取得しています。投票時に見えるのは前売りオッズで、
同じ値ではありません。**前売りオッズに当てはめて102.3%が出る保証はありません。**

第3段階として、前売りオッズと確定オッズの差を先に測ります。それまでは外します。

### 買う量の目安

9車立てだけなら **1日あたり約2.6レース**（実測2,252R ÷ 619日）。
1点100円なら1日260円、月約8,000円。

---

## 5. WINTICKET の操作 ★採取が必要

**この文書を書いた環境からは winticket.jp に到達できませんでした。**
セレクタはご自身のPCで採取してください。競艇版と同じやり方です。

```
python -m playwright codegen --channel=chrome https://www.winticket.jp/keirin/
```

手で操作するとコードになって出てくるので、対応する項目を
`winticket_page.py` の `SELECTORS` に写します。
**そのファイルだけ**をサイト変更時に直せばよい形にしてください（競艇版と同じ）。

### 採取するときに記録しておくこと

| 項目 | メモすること |
|---|---|
| ログイン | 方式（メール/SMS/2段階認証）。セッションがどれだけ保つか |
| 場・レースの選び方 | URL直打ちで行けるか、画面の中を辿る必要があるか |
| 賭式の選択 | **三連複**をどう選ぶか |
| 買い目の入力 | 3車をどう指定するか。ボックス指定になっていないか |
| 金額 | 100円単位か、口数指定か |
| 確認画面 | 合計金額・点数がどこに出るか |
| 残高 | 不足のときどう出るか |
| 締切 | レースページのどこに出るか（§3で使う） |

### 競艇側でハマったこと（同じ罠がある可能性）

- **URL を直接開くとログインが切れる**ことがある。画面内のリンクで移動する
- **ベットリストに買い残りがあると、追加しても確認画面に載らない**。dry のあとは片付ける
- **読み込み中はクリックが吸われる**。押す前に通信が落ち着くのを待つ
- **入力欄はクリックしてから入れ、入ったかを読み返す**
- **バッジの数字は文字として読めない**ことがある。件数ではなく中身で確かめる

---

## 6. 二重投票を絶対に防ぐ

**投票した瞬間に記録すること。** 確認画面を閉じてから書くのでは遅い。

```
bet_done.json:  {"20260921_富山_4R": {"ticket":"3=5=6","yen":100,"at":"10:20:11"}}
```

- キーは `日付8桁_場名_レース番号`（`races.json` の `key` に日付を足したもの）
- 起動時に読み込み、**そのキーがあれば何があっても買わない**
- 書き込みは投票ボタンを押した直後。例外が飛んでも書く（`finally`）
- **記録に失敗したら、そのレースは以後スキップする**（買ったか不明なら買わない）

`races.json` は毎回まるごと置き換わるので、**同じレースが何度も現れます。**
この記録が唯一の歯止めです。

---

## 7. 止める仕組み（必ず全部入れる）

| 仕組み | 既定 | 意味 |
|---|---|---|
| `i_have_read_the_terms` | `false` | true にするまで live は動かない |
| `--mode dry` | 既定 | 確認画面まで進んで投票しない |
| `max_yen_per_day` | 1000 | 1日の上限。超えたら以後買わない |
| `max_races_per_day` | 10 | 念のため |
| `STOP` ファイル | — | 置いたら次の周で止まる |
| `bet_yen` | 100 | 1点あたり |

**上限は「使いすぎ防止」ではなくバグ避けです。** ループの暴走や二重投票の
判定ミスで残高を一度に溶かさないための保険。9車立てだけなら1日2〜3レースなので、
`max_races_per_day: 10` に当たったら**何かおかしい**と思ってください。

---

## 8. 競艇版との両立 ★できます

`browser.py` は `launch_persistent_context(user_data_dir=...)` を使っています。
**プロファイルごとに別のChromeインスタンス**になるので、両方同時に動かせます。

### 条件

**別のフォルダに置くこと。** これだけ守れば競合しません。

```
C:\bet\auto_bet\       ← 競艇（既存）
  chrome_profile\
  bet_done.json
  config.json          profile_dir: "chrome_profile"

C:\bet\keirin_bet\     ← 競輪（新規）
  chrome_profile\      ← 別フォルダなので別インスタンス
  bet_done.json
  config.json          profile_dir: "chrome_profile"
```

`profile_dir` などは config.json からの相対パスなので、**フォルダを分ければ
設定値は同じままで衝突しません。**

### ★やってはいけないこと

**同じフォルダに置く / config.json をコピーして `profile_dir` を同じにする。**
`launch_persistent_context` は同じプロファイルを2つ同時に開けません。
競艇版の `browser.py` にもそのエラーメッセージが入っています
（「同じプロファイルは2つ同時に使えません」）。

### 実際に動かすとどうなるか

- **Chrome のウィンドウが2つ開いたまま**になります。それぞれ手でログインが要ります
- メモリは Chrome 2つぶん（それぞれ数百MB）
- ポーリングは2分に1回ずつなので CPU はほぼ使いません
- 開催時間は重なります（競輪は朝8時台〜深夜のミッドナイトまで）。
  **別プロセスなので、片方が落ちてももう片方は動き続けます**
- 投票が同時刻に重なっても問題ありません。別のChromeで別のサイトを操作するだけです

---

## 9. ファイル構成

```
keirin_bet/
  README.md
  config.example.json
  keirin_bet.py         ← 新規。主ループ（競艇の auto_bet.py を写して直す）
  race_source.py        ← 新規。races.json を読む（competition の history_source.py 相当）
  selector.py           ← 新規。plan.hot かつ not needOdds を選ぶ（競艇版より簡単）
  winticket_page.py     ← 新規。サイト操作。★採取が要る
  browser.py            ← 競艇版をそのまま流用
  bet_store.py          ← 競艇版をそのまま流用（二重投票の記録）
  betlog.py             ← 競艇版をそのまま流用
  config.py             ← 競艇版を流用。キーだけ差し替え
  jst.py                ← 競艇版をそのまま流用
  tests/                ← 競艇版に倣う
```

**`winticket_page.py` が一番大きくなります。** 競艇の `telebote_page.py` は754行でした。

---

## 10. config.example.json

```json
{
  "_1_必ず読むこと": [
    "このファイルの i_have_read_the_terms を true にするまで、実際の投票はしません。",
    "WINTICKET の利用規約に自動操作の扱いが書かれています。",
    "自分で読んで、問題ないと判断したときだけ true にしてください。"
  ],
  "i_have_read_the_terms": false,

  "_2_上限": "バグ避けの保険。9車立てだけなら1日2〜3レースなので、10に当たったら何かおかしい",
  "bet_yen": 100,
  "max_yen_per_day": 1000,
  "max_races_per_day": 10,

  "_3_どれを買うか": [
    "needOdds が true(7車立て)は第3段階まで買わない。",
    "前売りオッズと確定オッズの差が未検証のため。"
  ],
  "buy_need_odds": false,

  "_4_締切": [
    "startTime(発走)の何分前から見に行くか。締切自体はサイトから読む。",
    "締切が読めなかったら買わない。"
  ],
  "lookahead_minutes": 30,

  "_5_取得元": "keirin が書いた races.json を読むだけ（書き込みはしない）",
  "races_url": "https://raw.githubusercontent.com/honda1986/keirin/main/races.json",
  "poll_seconds": 120,

  "_6_ブラウザ": "profile_dir は .gitignore に入れて、絶対に共有しないこと",
  "profile_dir": "chrome_profile",
  "use_installed_chrome": true,
  "headless": false,

  "_7_WINTICKET": "セレクタは winticket_page.py の SELECTORS に codegen で採取して入れます",
  "winticket_url": "https://www.winticket.jp/keirin/",

  "_8_置き場所": "config.json のあるフォルダからの相対",
  "bet_done_path": "bet_done.json",
  "log_path": "logs/keirin_bet.log",
  "shot_dir": "shots",
  "stop_file": "STOP"
}
```

---

## 11. 作る順番

1. **`race_source.py` + `selector.py` だけ作って、買うべきレースを画面に出す**
   （ブラウザは触らない。ここで `plan` の読み方が正しいか確かめる）
2. **`winticket_page.py` のセレクタを codegen で採取**
3. **`--mode dry`** で確認画面まで進むのを見る。**投票ボタンは押さない**
4. 何日か dry で回して、**買うべきレースが正しく選ばれているか**を目視で確認
5. `i_have_read_the_terms` を `true` にして **`--mode live`**
6. 最初の数日は画面を見ていること

**4 を飛ばさないでください。** 選び方が間違っていても dry では気づけますが、
live では気づいたときには買っています。

---

## 12. 未検証・わかっていないこと

この文書の弱いところを正直に書いておきます。

- **WINTICKET のページ構造・ログイン方式・規約をいっさい確認できていない。**
  §5 は競艇側の経験からの推測を含みます
- **競輪のネット投票の締切が発走の何分前かを確認していない。**
  だからサイトから読む設計にしてあります（§3）
- **前売りオッズと確定オッズの差が未測定。** 7車立てを外している理由（§4）
- **残高不足・投票拒否・メンテナンス中の挙動が不明。** 実装時に確認して追記してください
- **回収率104.7%の95%区間は96.7〜113.7%で、100%をまたぐ。**
  自動化しても、それが利益を生むとは確定していません

---

## 13. 第3段階（7車立て）に進む条件

前売りオッズを記録して、確定オッズとの差を測ってから。

1. `odds.js` を**締切前にも**回して前売りオッズを別ファイルに記録する
2. 数ヶ月ぶん貯める
3. 「前売り4〜15倍」で絞ったときの回収率を、確定オッズ版の102.3%と比べる
4. 大きく落ちないことを確認してから `buy_need_odds: true` にする

**第2段階（9車立て）が安定して動いてから着手してください。**
