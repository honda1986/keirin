name: 評価値
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: 評価値
        run: |
          node verify.js
          echo ""
          echo "########## 診断 ##########"
          echo ""
          node diagnose.js
