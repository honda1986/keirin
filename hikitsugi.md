# 競輪予想アプリ 引き継ぎ資料

最終更新: 2026-09-25 / リポジトリ: `github.com/honda1986/keirin`
前回の資料: 2026-09-21（全面改訂版。2026-09-25 に §4-2 と各所の古い記述を更新）

---

## 0. 最初に読む人へ

**この資料の内容が現状と食い違っていたら、資料ではなく実データを信じてください。**
過去に2回、「引き継ぎメモの次タスクが既に完了済み」という事故が起きています。
作業開始前に必ず実際のファイル・ログを確認してください。

### 作業環境

- はむさんは普段 **Android + GitHubのWeb画面** で操作する。
  Windows PC もあり、競艇の自動投票（`honda1986/v24` の `auto_bet/`）をそこで動かしている。
- **Claudeはリポジトリに直接pushできます**（2026-09-21から）。Colabでの手動差し替えは不要。
  - セッション開始時に push 権限でリポジトリを追加すること。
  - 作業はブランチを切って PR にする。マージは指示を仰ぐ。
- アプリのHTML/JSを差し替えたあとは **ブラウザのキャッシュクリア（再読み込み）が必要**。
- 検証用スクリプトは GitHub Actions のワークフロー「評価値」(`diagnose.yml`) の
  `cmd` 欄に `node xxx.js` と入れて実行できる。読み取り専用（`permissions: contents: read`）。

### この環境でできないこと

- **`keirin.kdreams.jp` への通信はネットワークポリシーで塞がれている**ことがある。
  ローカルでは実取得を検証できないので、GitHub Actions で回すこと。
- git履歴・history.json・odds-*.json を使った検証はローカルでできる。

---

## 1. システムの全体像

```
GitHub Actions
  main.yml     1日4回  fetch.js    → races.json(買い目 plan 入り) + scores.json
  live.yml     日中1時間おき live.js → results-today.json(今日の着順・配当。history には触らない)
  results.yml  1日1回  results.js → gitfill.js(直近3日の漏れ) → sanpuku.js → results.js --stats-only
                       → history.json + stats.json(成績・今日の結果・直近7日の🔥一覧)
  odds.yml     月1回   odds.js     → odds-YYYYMM.json
                        ↓
              index.html (GitHub Pages) で表示
```

- `index.html` は静的HTML。JSONを読んで表示する。
  **engine.js と同じ予想ロジックを内部に複製して持っている**。
  → **engine.js を直したら index.html 側も必ず同じ修正が要る**。
- 予想の計算は `engine.js` の `parseCard` → `applyScoreLog` → `predict` → `sujiExpect`。
- 買い目の判定は `f3PlanFrom`。**engine.js と index.html の両方にある**（#12 で engine.js にも移した）。
  `fetch.js` が `races.json` の各レースに `plan` として書き出す。
  **共有している6関数（parseCard / parseKdreams / predict / applyScoreLog / prevMeetScore / f3PlanFrom）は
  バイト単位で同一であること**。直したら両方直し、一致を確かめる。

### 主要ファイル

| ファイル | 役割 |
|---|---|
| `fetch.js` | 当日の全レースを取得 → `races.json`。ついでに `scores.json` も更新 |
| `engine.js` | 出走表パーサ（`parseGamboo` / `parseKdreams` / `parseWinticket`）と予想エンジン |
| `index.html` | アプリ本体（engine相当のコードを内包 + 買い目判定 `f3PlanFrom`） |
| `results.js` | 当日結果の取得 → `history.json` に追記 |
| `odds.js` | 三連複オッズの取得 → `odds-YYYYMM.json` |
| `scorelog.js` | 競走得点の日次ログ → `scores.json`（前得点の代用に使う） |
| `live.js` | 今日の結果（着順・3連複/3連単/2車単の配当）→ `results-today.json`。アプリが当日の的中・配当を出すのに使う |
| `gitfill.js` | git履歴の `races.json` から history の穴を埋める |
| `tokufill.js` | git履歴から `riders` に競走得点を後付けする（一度実行済み） |
| `learn.js` | 実測から採点補正を学習 → `weights.json` |
| `simulate.js` | 買い目パターンの総当たりバックテスト → `sim.json` |
| `backfill.js` | 過去レースの一括取得。**Gamboo依存なので現在は使えない**（§6） |
| `bankdata.js` | 競輪場データ（`TRACK_NAMES` の順序が `VENUE_PIDS` と対応） |
| `diagcard.js` / `diagfetch.js` | 使い捨ての診断スクリプト（毎回書き換える） |
| `AUTOBET_SPEC.md` | 自動投票（オッズパーク）の仕様書。**実装は保留中**（§9） |

---

## 2. データの現状

### history.json（22MB）

- **44,787レース** / 2025-01-01 〜 2026-09-20（2026-09-25 時点で 45,010）
- **日付の形式は `YYYYMMDD`**（`20250101`）。`YYYY-MM-DD` ではない。
- エントリのキー:
  `id,date,place,raceNo,klass,grade,score,verdict,pattern,f,s,t,p3pay,p2pay,suji,`
  `honmeiWin,honmeiRen,n2cnt,n2hit,n3cnt,n3hit,ranks,gap,riders,lines,p3fpay`

**★最大の地雷: 払戻のフィールド名**

| フィールド | 中身 | オッズ×100 との一致率 |
|---|---|---|
| `p3fpay` | **三連複**の払戻 | **99.993%**（42,554件中42,551件） |
| `p3pay` | 三連単の払戻 | 0.097% |
| `p2pay` | 二車単の払戻 | 1.1% |

三連複を測るときは必ず `p3fpay`。`p3pay` のほうが名前として自然なので間違えやすい。

**★地雷その2: `p3pay`（三連単）は荒れたレースほど欠けている**（2026-09-25 に発見）

| 三連複の配当 | `p3pay` の欠損率 |
|---|---|
| 500円未満 | 0.8% |
| 3,000〜10,000円 | 56% |
| 10,000円以上 | 66% |

2026年3月までは約24%が欠けていて、4月以降は0%。**`p3pay != null` で絞ると荒れたレースが抜け、
本命寄りの買い方が大きく良く見える**（例: 🔥の3単2点は `p3pay` がある分だけで測ると2025年 127.0%、
全レースを使い欠けを furoito で補うと 103.1%）。
- 3連単を測るときは、**外れたレースは配当が無くても戻り0で確定**なので全レースを使う。
  不明になるのは「当たったのに配当が無い」レースだけ。
- 欠けている配当は furoito/keirin-data（下記）で **7,671件中7,640件** 埋まる。
- **`simulate.js:408` がこの絞り方をしている**（`e.p3pay == null` を飛ばす）。
  `sim.json` の3連単構成の成績は上振れしている。詳細画面の「学習した最良構成」はこの数字。

**落とし穴: `ranks` は印6つ分しか入っていない。**
全選手・車立てを見たいときは必ず `riders` を使う。

**`riders` の中身**（2026-09-21から7項目）:
```
[車番, 年齢, 期, ライン内位置(0先頭/1番手/2三番手+/3単騎), 評価順位, 評価点, 競走得点]
```
- **競走得点(7番目)は 2026-08-28 以降のデータにしかない**（1,810件）。
  それ以前は Gamboo期の raw が git履歴に残っていないため復元できない。
  読む側は `rd[6]` が `undefined` でも動くようにすること。
- **評価点(6番目)は採点の総合点**で、競走得点とは別物。

### odds-YYYYMM.json（21ヶ月分・約10MB）

- `{updatedAt, races: {"<id>": {cars, o:[...]}}}`
- `o` は三連複の組み合わせを昇順に並べた順番（`[1,2,3],[1,2,4],...`）
- **検証済み: 的中組のオッズ × 100 === `p3fpay`**（42,554件中42,551件で一致）。
  オッズは「倍」、払戻は「円/100円」。
- **月1回 自動取得**（毎月1日 11:00 JST、直近45日）。当日は含まない＝確定オッズのみ。

- **`o` の中に `0` や `null` が混ざることがある**（欠車の組など）。`1/o` をそのまま取ると
  NaN になり、グループ全体の集計が壊れる。0/null は除いてから正規化すること。

### scores.json（295KB）

- `{updatedAt, dates, riders: {"<名前>|<期>": [["YYYYMMDD", 得点], ...]}}`
- 2,266人 / 25日（2026-08-28〜）。`fetch.js` が毎回追記する。
- **得点は毎日は動かない。同じ開催中(間隔2日以内)は99.7%が同じ値**で、
  更新は開催をまたぐときに起きる。差は前日ではなく**前回開催**を基準に取る。

### 外部データ: furoito/keirin-data（2026-09-25 に発見・検証）

`github.com/furoito/keirin-data`（公開）。Kドリームスから集めた**選手1人×1レースで1行**の月別CSV。
`Kenseimk/keirin-data` はバイト単位で同じ複製。

- **2022年1月〜**。結果ありは 2022年 26,619 / 2023年 27,457 / 2024年 27,717 / 2025年 27,395 レース
- 列: 着順・車番・脚質表記・競走得点・期・年齢・級班・ギヤ・**上がりタイム**・**そのレースの決まり手**・
  **展開の短評**（`win_lose` 列: 突張り先行 / 先行捲られ / 目標捲られ / 道中千切れ など）・2単/3単/3複の配当と人気
- **並び（ライン）は無い**。並びは history.json の `lines` と結合して使う
- **S・B（誰がSを取ったか）の実績も無い**
- history.json と結合: 2025年1月以降 **38,183レース、1着一致 99.97%、3単配当一致 99.93%**

**落とし穴**
1. **`date` 列はレース日ではなく開催初日。** 16桁の `race_id` = 場(2)+開催初日(8)+何日目(2)+R(4)。
   レース日 = 開催初日 + (何日目−1)。そのまま使うと2日目以降が全部ずれる（results.js #1 と同じ罠）
2. **2026年5月以降は着順が空欄のレースが大半**（9月はほぼ全部）。使えるのは2026年4月まで
3. 決まり手の「ク」は「マーク」が途中で切れたもの
4. 短評はレースの**約3分の1**にしか付いていない（付いているレースでは全員に付く）
5. **ライセンスの記載が無い。** 分析に使うだけにして、CSV を honda1986/keirin にコピーしないこと。
   分析のたびに clone する（`GIT_LFS_SKIP_SMUDGE=1 git clone --depth 1 https://github.com/furoito/keirin-data`）

---

## 3. 現在の買い目戦略

前提: 控除率25% → **回収率75%が「平均的な買い方」の期待値**。100%を超えて初めてプラス。

### 買い目: 三連複・本命ラインの先頭3人（1点）

`index.html` の `f3PlanFrom(ranked, lines)` が判定する。
評価1位が属するラインの**先頭3人**（隊列順）を買う。

| 条件 | R数 | 的中率 | 回収率 | 95%区間 |
|---|---|---|---|---|
| **9車立て(8車以上)・単騎1人以下 → 買う** | 2,252 | 17.8% | **107.3%** | 94〜122% |
| **7車立て・順位合計12以上 × オッズ4〜15倍 → 買う** | 2,535 | 14.8% | **102.3%** | 92〜113% |
| 7車・順位合計12以上だが帯の外 | 1,860 | 20.5% | 87.7% | 73〜103% |
| 7車・順位合計12未満 | 15,968 | 32.6% | 82.6% | 80〜85% |
| 6車以下 | 1,282 | 37.1% | 80.0% | 72〜88% |
| 9車立て・単騎2人以上 | 235 | 13.6% | 60.0% | 37〜84% |
| 全部買った場合 | 24,132 | 28.5% | 87.0% | 84〜90% |
| **★買う2条件の合計** | **4,787** | **16.2%** | **104.7%** | **96.7〜113.7%** |

1日あたり **7.7レース**（619日で4,787R）。100円/点なら月約23,500円。

**順位合計**＝トリオ3人の評価順位の合計。`_lineRankSum` / `f3PlanFrom` の `rankSum`。

### ★この数字の弱いところ（必ず読むこと）

1. **95%区間が100%をまたぐ。** 買う2条件の合計で 96.7〜113.7%。
   4,787レース積んでも「負けている可能性」を排除できていない。
2. **利益が1四半期に集中している。** 総収支+22,320円のうち
   **2025Q3だけで+15,050円（67%）**。除くと4,097Rで101.8%。
3. **移行後（2026-08-28以降）のデータは24日・147レースしかない。**
   Kドリームス由来の入力でも同じ数字が出るかは**まだ確認できていない**。
   1日7.7Rなので、区間を±10ptに狭めるには約8ヶ月かかる。

4. **2026年に入って落ちている**（2026-09-25 に furoito で欠けを補った全データで確認）

   | 期間 | 🔥 3複1点 |
   |---|---|
   | 2025年 | 103.1% |
   | 2026年1〜4月 | 94.7% |
   | 2026年7/5〜9/24 | 88.3% |

   9車は 2025年 112.3% → 2026年 100.1%、7車（帯なし）は 98.3% → 92.7%。
   成績カードの累計（9車 107.3%）は、ほぼ2025年の貯金。

**「104.7%で勝っている」と断定しないこと。** 止めるべき兆候も出ていないが、
確立したとも言えない。月次で区間付きの表を見ていくのが実務的。

### 7車立てはオッズ確認が必須

| 7車・順位合計12以上 | R数 | 回収率 |
|---|---|---|
| 帯内（4〜15倍） | 2,535 | 102.3% |
| 帯外 | 1,860 | **87.7%** |
| 確認せず全部買う | 4,395 | **96.1%** |

**9車立ては帯を外しても102.6%で成立するが、7車は帯が生命線。**
アプリは両者を別セクションに分け、7車には赤枠で警告を出している（`oddsGate`）。

### 市場のオッズの癖（1,626,730組で計測）

エッジ比 = 回収率 ÷ 75%。

| オッズ帯 | 回収率 | エッジ比 |
|---|---|---|
| 1.5〜30倍 | 77〜83% | 1.03〜1.10（市場は正確） |
| 50〜100倍 | 55.4% | 0.74 |
| 100〜300倍 | 35.8% | 0.48 |
| 300倍〜 | 16.3% | 0.22 |

**「当たりにくい買い目ほど回収率が高い」は誤り。** 実オッズで反証済み。

---

## 4. 2026-09-21 にやったこと

### 障害の発見と復旧

**結果の取り込みが 2026-08-26 から止まっていた（約27日・1,810レース）。**
`results.js` がレース日を Gamboo の URL（`rdt=`）からしか取れず、
8/28 の Kドリームス移行で `rdt=` が消えたため。
`results update` のコミットは毎日走っていたが `stats.json` のタイムスタンプだけが
動いていた。→ PR #1 で修正、#2 の `gitfill.js` で git履歴から1,810件を復旧。

### 直したバグ

| | 内容 | 影響 |
|---|---|---|
| #1 | `results.js` のレース日取得 | 27日分の欠測 |
| #3 | `simulate.js` が `ranks` を使い車立てを常に6として計算 | 100%超の3連複構成を1つも発見できていなかった |
| #3 | `r[6]` が `undefined` のまま買い目に入っていた | 44,787件中42,155件(94.1%)。「4-7位」系の的中が3,845→5,681に |
| #5 | ギヤが2つ並ぶ選手の競走得点を誤読 | 13,421人中32人。得点3.9xで最下位に落ちていた |

### 足したもの

| | 内容 |
|---|---|
| #4 | 7車🔥に「買う前にオッズを確認」の警告 |
| #6 | `scorelog.js` / `scores.json` — 競走得点の日次ログ |
| #7 | `scoreDiff` の復元（移行以降ずっと0だった） |
| #8 | 位置×年齢の交互作用（`posAgeBonus`） |
| #9 | オッズの月次自動取得 |
| #10 | `riders` に競走得点 |

### #8 の要点 — 学習の的を変えた

`posBonus`/`ageBonus` は**1着率**の乖離から作っている。だが買っているのは
三連複で、当たるかは**3人が揃って3着以内に入るか**で決まる。的が違った。

**実測: ライン3人それぞれが単独で3着以内に入った率**

| | 3着以内率 |
|---|---|
| 先頭 | 65.0% |
| 番手 | 64.9% |
| **3番手** | **26.2%** |

3人中2人だけ的中した2,734レースで、**落ちた1人は76.6%が3番手**。
この買い目は実質「3番手が3着以内に来るか」に賭けている。

**位置×年齢は掛け合わせないと表せない**（3着以内率の乖離）:
```
単騎 28-35歳  +1.8pt      先頭 〜23歳   +4.2pt
単騎 50歳〜  -11.6pt      先頭 44歳〜   -8.1pt
```
単騎は年齢で符号が逆になる。加法項では表現できない。

**1着率を的にした版は失敗した**（104.3%→103.9%、学習期間でも悪化）。
的を3着以内率に変えたら 104.3%→106.3%。
**何を足しても改善するなら過学習を疑うべきで、失敗したことが診断の裏付けになった。**

## 4-2. 2026-09-22〜25 にやったこと・わかったこと

### PR

| | 内容 |
|---|---|
| #12 | `f3PlanFrom` を engine.js に移し、`fetch.js` が `races.json` に `plan` を書く |
| #13 | `AUTOBET_SPEC.md`（オッズパークの自動投票の仕様書。実装は保留） |
| #14 | `main.yml` の朝の取得を前倒し。**GitHubの定時実行は実測1〜7時間遅れる**。06:30予定が08:15頃に走り、9/22は始発(08:30)に間に合わず前日の買い目が出たままだった |
| #15 | 成績カードを `sim.json` の古い構成から、いま買っている3連複「本命ライン3人」の実測（`stats.json` の `f3`）に差し替え |
| #16 | アプリに本命ライン先頭の脚質を表示。逃げ型のときは3単「先頭-番手-3番手」を参考表示 |
| #17 | 的中率をレースごとの推定に（`f3HitProb`）。下の「的中率の推定」 |
| #18 | 画面の作り直し（下のタブ・自動読み込み・枠色の車番・状態表示）と、当日の結果表示（`live.js`）。**9/23 の取り込み漏れ**の対策（下の「結果の流れ」） |
| #19 | **締切の表示を直す。** `races.json` の `startTime` は発走時刻で、締切は**発走の5分前**。カードの大きい時刻を締切にし、「締切まで○分」も締切までを数える（以前は発走までを数えていた） |
| #20 | `live.yml` を「起動したら10分おきに取り続け、時間切れで自分を起動し直す」形に。9/23 の原因の説明を訂正 |
| #21 | 成績タブを**日別カレンダー**に（`daily.json`・全期間）。`live.yml` が1時に止まるとき `results.yml` を起動（23:50 の定時実行が毎日3時台にずれ、日別成績が翌朝まで変わらなかった） |

### 結果の流れ（#18）

- **日中**: `live.yml` が `live.js` を**10分おき**に回し、`results-today.json` に今日の結果を書く（#20）。
  取得失敗時は書かない／同じ日の結果は減らさない。アプリは5分おきに読み直す。
  ★**GitHub の定時実行は当てにならない**ので、1回起動したらその中で10分おきに取り続け、
  5時間40分（上限6時間の手前）で区切ったら**自分で次の実行を起動し直す**（`gh workflow run`、`actions: write`）。
  深夜1時〜朝8時（JST）は起動し直さずに止まる。朝の起動は 08:07 JST ＋予備3回（動いている最中なら順番待ちになるだけ）。
  最初の版（1時間おきの定時実行）は、9/25 の手動実行のあと**一度も起動しなかった**
- **夜**: `results.yml` が `results.js` → **`gitfill.js`（直近3日の漏れ）** → `sanpuku.js` → `results.js --stats-only`。
  起動は `live.yml` が1時に止まるとき（#21）＋ 23:50 の定時実行（予備。実際は毎日3時台にずれ込んでいた）
- **`daily.json`**（#21）: `results.js` が全期間の日別🔥を書く。1件 = `[場, R, 車立て, 買い目, 着順, 的中(1/0/-1=3着不明), 3複配当, 買い目の確定オッズ]`。
  アプリのカレンダーはこれ＋今日の速報（`SHOUBU_ROWS`×`results-today.json`）。7車は確定オッズが4〜15倍の外なら「見送り」で数えない（切替可）。
  過去分は「いまの買い方を当時の予想(history の riders/lines)に当てはめた」さかのぼり計算で、🔥ルールを決めた期間を含む＝見かけより良く出る
- アプリは `results-today.json` と `stats.json` の `todayRaces` を、**`races.json` のレース日と一致するときだけ**使う
- **9/23 の結果が丸ごと欠けていた。** 9/23 分の取り込みは 9/24 03:39 JST に `races.json` が9/23分のまま走ったが、
  **Kドリームスの払戻ページが1レースも返さず**（掃除・取り込み・3連複の3つとも0件）、黙って0件で終わっていた。
  （#18 の時点では「朝の取得が先に races.json を書き換えたため」と説明したが、ログを見たら誤りだった。
  ただしその形の取り逃がしも起こりうる：`results.js` は「いまの `races.json` に載っている日」しか見ない）
  どちらの場合も、`gitfill.js` を毎晩直近3日で回して埋める（点検では 9/23 の66R すべてが復旧対象と確認済み）
- `stats.json` の「今日」は実行時刻ではなくレース日。`todayRaces` に3連複配当 `p3fpay`、`recentHot`（直近7日の🔥1件ずつ）を追加

### 締切前オッズの記録（#23・`snap.js` / `snap_pack.js` / `snap_eval.js` / `snap.yml`）

競艇 v24 の「判定時オッズの保存」（v24 の引き継ぎ資料 §2-4・§2-5）を競輪に移したもの。
**手元の過去オッズは確定オッズだけ**なので、オッズで買い目を絞る改善（7車の帯・§4-3 の期待値）は
「確定オッズで選んで確定オッズで払う」形でしか測れていない。本番は締切前のオッズで選ぶ。その差を測るための記録。

- **何を**: 3連複の**全組**（9車84組・7車35組・6車20組）。並びは `odds-YYYYMM.json` と同じ昇順
- **いつ**: 締切（発走の5分前）の**15〜2分前**のレースを、**3分おき**に（1レース4〜5回）。買う/買わないに関係なく全レース
- **どこに**: `odds-snap` ブランチの `snap/YYYYMMDD.json.gz`（30分おきにまとめて push）。
  main に入れないのは、3分おきの commit で GitHub Pages の作り直しと履歴の膨らみが起きるため
- **1行の形**: `{t:取得時刻, k:"熊本_7R", rid, left:締切まで何分, upd:画面の「HH:MM現在」, n:車立て, o:[倍率…]}`

**PC が主、GitHub が予備（#25・`pc/`）** — 競艇 v24 と同じ形（v24 の PC 引き継ぎ資料）。詳しい手順は `pc/README.md`。
- Windows のタスクスケジューラ `keirin_snap` が **07:50〜翌01:00 に1分おき**、`pc\task.bat` → `node pc\runner.js snap` を1回ずつ動かす
  （1回ごとに終わる。落ちても失うのは1分ぶん）。フォルダは `C:\keirin\keirin`（リポジトリ）と `C:\keirin\logs`
- runner.js: ロック（20分より古いものは捨てる）→ 10分おきに main を取り込む（コードを手で直しかけていたら races.json だけ）→
  races.json が今日でなければ `fetch.js` を `RACES_OUT=snapwork/races-local.json` で動かして自前で出走表を取る（main は汚さない）→
  `snap.js` → 10分おきに odds-snap へ送る（**リモートを土台に置き直してから `snap_pack.js` で足し合わせる**。rebase/force は使わない）＋心拍
- git には「聞かない」環境変数（`GIT_TERMINAL_PROMPT=0` `GCM_INTERACTIVE=never`）。ログインは `pc\setup.bat` の画面のある窓で1回
- GitHub の `snap.yml` は心拍が25分以上古いと引き継ぐ。**切り替えの操作は不要**（記録は足し合わせるだけで、通知も投票もしないので二重に動いても壊れない）
- 確かめ方: `pc/selftest.js`（外に触らず、送信・GitHub 側との足し合わせ・ロック・送れないときの持ち越し・手で直しかけの保護を確認）

**取得元の下調べ（2026-09-26）**
- Kドリームスのレース詳細のオッズ画面（`?pageType=odds&kakeshikiType=3renhuku`）。普通の HTML で、1ページ約1秒・20〜30万文字
- 3連複は「**人気順**」「**高配当順**」の2つの一覧に出る。**どちらも上位50組まで**なので、9車は片方では足りない。
  2つを合わせると全組そろう（`parseTrio`）。見出しやタブにも「人気順」の文字があるので、「人気順 1 a=b=c」と続く所から読む
- 確定済みの90レース（9車11レースを含む）で、読んだ倍率が `odds-YYYYMM.json` と**全組一致**
- Σ(1/オッズ) は確定で **約1.34**（払戻率75%。競艇の3連単 1.337 とほぼ同じ）
- **前売り（票が少ない時点）は 9999.9 が多い**（前日深夜で 17〜61組）。記録ではそのまま残し、使う側で無効として扱う
- 3連単は「人気順」が上位100組までしか出ない（全組は表の形で出ているが未対応）

**まだ分からないこと**（たまってから `node snap_eval.js <snapのディレクトリ>` で見る）
- 確定 ÷ 締切前の倍率（v24 では中央 0.96）と、7車の帯（4〜15倍）が締切前→確定でひっくり返る件数
- 締切の何分前まで倍率が動くか（画面の「HH:MM現在」の更新間隔）
- 締切を「発走の5分前」としているのは仮定。**画面の締切表示とずれていないか**も記録の `upd` と `left` で確かめる

### 買い方の総点検（13候補を事前宣言・2025年で作って2026年で測定）

- **確実に100%を超える買い方は見つからなかった。** 2026年の95%区間が100%を下回らないものは無し
- 市場（3複の確定オッズ）には**本命側が過小評価される偏り**がある（確率の指数 1.18）
- 市場＋ライン構造のモデル（Benter型）は、ライン構造の情報が市場と評価順位でほぼ織り込み済みで、
  EV で買う方法（EV≥1.0の全組 / 最良1点）は年によってひっくり返った
  （2025年 97.7% / 93.3%、2026年 110.1% / 131.1%。モデルを作った2025年のほうが悪い）。採用せず

### 3連単（欠けていた配当を furoito で補ってやり直し）

- **全体では3連複より悪い**（全レース 3単 先頭-番手-3番手 87.8/86.1% ／ 3複1点 87.7/85.3%、2025/2026）。6点BOXは60%台
- **例外: 先頭が逃げ型なら「先頭-番手-3番手」1点が3複1点より良い。** 2025年で決め、別の2期間で確認
  （95.7 / 98.1 / 97.3% 対 88.4 / 89.4 / 91.6%）。**ただし100%未満**なので参考表示のみ
- 「捲り型なら番手-先頭-3番手」は3期間目（7〜9月）で 70.6% に崩れた。使わない

### 脚質（本命ライン先頭の型）

判定: 決まり手欄（直近の1〜2着の決まり手回数）の 逃げ+捲り=J。
J≤2 自力弱 / 逃げ÷J≥0.6 逃げ型 / ≤0.4 捲り型 / 他 両立型。
過去分は furoito の決まり手から「直前120日」の回数を作った（出走表の回数と相関 0.84/0.78）。

| 先頭 | 3複本命ライン3人の的中 2025 | 2026年1〜4月 | 回収率 2025/2026 |
|---|---|---|---|
| 逃げ型 | 32.3% | 32.4% | 88 / 89% |
| 両立型 | 30.9% | 27.3% | 92 / 86% |
| 捲り型 | 23.9% | 19.4% | 83 / 80% |
| 自力弱 | 21.2% | 19.7% | 89 / 65% |

- **的中率への効き目は確か**（76日版でも16か月でも同じ向き）。**市場もかなり織り込んでいて回収率の差は数ポイント**
- しくみ（短評で確認）: 逃げ型の先頭は実際に主導権を取る率 46%（捲り型 21%）。
  捲り型は後方に置かれる率 3.8%（逃げ型 1.1%）。**番手が離れた率は型によらず6%台** —
  「捲り型は番手が千切れる」は誤りだった
- 76日版で見えた「相手ラインに逃げ型が1人いると市場が売りすぎ(103%)」「🔥を逃げ型に絞ると100% vs 80%」は、
  **16か月では再現しなかった**（前者は2025年 80.9%、後者は 108.7 vs 99.1% / 96.5 vs 93.2% と差が小さい）

### S（スタート）と車番（76日ぶん・出走表のみ）

- **S回数だけでは差が無い**（本命ラインのS取り役のS回数で分けても 27〜29%）
- **ライン内のS上手（S回数最多）の車番と組み合わせると差が出る**:
  S3回以上×内(1〜3番車) 的中31.7%・スジ58.3% ／ S3回以上×外(4番車以上) 24.2%・47.2%
- 相手ラインのS上手が内だと、本命ラインは 19.3%（外なら 24.5%）
- 逃げ型は前を取れても取れなくても強い（34.4 / 31.2%）。**捲り型は前を取れないと 20.7% に落ちる**
- 回収率には効いていない（市場が織り込み済み）
- 「内＝1〜3番車」は仮の区切り。**レースごとのS取りの実績が無いので、車番ごとの取りやすさは直接測れていない**

### 的中率の推定（`index.html` の `f3HitProb` / `F3_HIT_MODEL`）

表示の的中率を、条件ごとの固定値（7車 14.8% など）から**レースごとの推定**に変えた。
- ロジスティック回帰。2025年 13,908R で学習（furoito 由来の競走得点・120日3着以内率・決まり手 ＋ history の評価点・並び）
- 特徴量14個: 9車立て / 単騎の人数 / 競走得点のレース内z（先頭・番手・3番手・別線で最強）/
  3連対率のレース平均との差（3番手・別線で最強）/ 評価点の差 / 先頭の脚質3種 / 2人以上の別線の数 / 順位合計÷車数
- 確認: 2026年1〜4月（furoito 由来）と 2026年7〜9月（**出走表そのもの**）で推定と実際がよく合う。
  🔥の中のAUC 固定値0.49〜0.52 → 0.65〜0.67。Python と JS の出力は 2,667R で完全一致
- **買う/見送るには使わない。** 「推定×確定オッズ」の期待値で選んでも期間ごとにばらばら
  （期待値1.2以上: 109% / 86% / 68%）。損益分岐オッズ・オッズ帯は条件全体の実測値のまま
- 競走得点・3連対率・決まり手のどれかが無い出走表では推定せず、固定値を出す
- **学び直すとき**は特徴量の順番を変えないこと（係数は並び順で対応している）

---

## 4-3. 2026-09-26 の調査: 市場（オッズ）が見落としている選手の特徴

**切り口を変えた。** 「当たる選手」ではなく「**3連複オッズが見込む3着以内率より、実際によく来る選手**」を探した。
市場の3着以内率 = その選手を含む全3連複の (1/オッズ) の和（全組で正規化）。オッズの高低による歪みは
スプライン（`bs(logit, df=6)` × 9車か）で取り除き、その上で特徴の係数を測った。
**2025年 / 2026年1〜4月 / 2026年5〜9月** の3期間。選手30.5万人・走（furoito ＋ history ＋ odds を結合）。
事前宣言した12項目のうち、3期間とも同じ向きで効いたもの:

| 特徴（ほかの特徴と同時に入れた係数・t値） | 2025 | 2026年1〜4月 | 2026年5〜9月 | 意味 |
|---|---|---|---|---|
| **評価順位が1つ下がるごと** | +0.052 (11.4) | +0.066 (8.4) | +0.081 (10.8) | **市場はこのアプリの評価上位を買いすぎ** |
| **得点の変化（今−90日以上前、1点あたり）** | −0.013 (−5.7) | −0.021 (−5.6) | −0.023 (−5.7) | 得点が**下がった選手は過小評価**、上がった選手は過大評価 |
| **上がりタイム（直近10走、レース平均との差・秒）** | −0.27 (−6.5) | −0.33 (−5.1) | −0.30 (−4.8) | 上がりが速い選手は過小評価 |
| 単騎 | −0.09 (−4.9) | −0.14 (−4.5) | −0.20 (−6.8) | 過大評価 |
| 番手以降（2人以上のライン） | −0.07 (−4.6) | −0.16 (−5.6) | −0.18 (−6.5) | 過大評価 |
| 30日以上の休み明け | −0.15 (−2.5) | −0.23 (−2.2) | −0.09 (−2.9) | 過大評価 |
| 50歳以上 | +0.08 (4.8) | +0.05 (1.7) | +0.05 (1.8) | やや過小評価 |

効かなかった／ぶれたもの: 地元（単独なら 実際/期待 1.03/1.02/1.03 だが、ほかと同時に入れると t 2.2/0.1/0.9）、
若手（期≥121）、同県の番手、前走1着、昇級・降級初戦、初日・最終日（どれも 1.00 前後）。

得点の変化の勾配（実際÷期待）: −4以下 1.035/1.035/1.080 → ±0.5 0.996/0.994/1.007 → +4以上 0.983/0.952/0.973。
**得点は4か月平均なので遅れて動き、市場はそれをそのまま信じている**、という読み。

**大きさ:** 1人あたりの差は数%。控除25%を1人分の特徴だけで超えることはない。

### 買い目への当てはめ（候補。採用はしていない）

2025年で「市場＋特徴」のモデルを作り（組の確率 ∝ (1/オッズ)^1.18 × exp(3人の特徴の和)）、
🔥の買い目の期待値＝確率×オッズ で分けた（7車は帯内のみ＝実際の買い方）。特徴は**出走表で手に入るものだけ**
（評価順位・単騎・番手・50歳以上・地元・若手・同県）:

| 🔥（実際の買い方） | 25Q1 | 25Q2 | 25Q3 | 25Q4 | 26Q1 | 26Q2 | 26Q3 |
|---|---|---|---|---|---|---|---|
| 期待値≥1 | 125.2 | 107.5 | 123.7 | 93.9 | 123.6 | 122.7 | 114.5 |
| 期待値<1 | 92.7 | 101.2 | 120.9 | 107.2 | 88.0 | 97.1 | 79.9 |

- 7期中6期で「≥1」が上。9車（119.7 対 100.3%）でも7車（114.1 対 96.9%）でも、順位合計13以上/未満の中でも差が残る
- 2026年だけ: ≥1 は570Rで約121%、<1 は1,332Rで約89%
- **効き目の大半は「評価順位」**（それだけでも 2026年 118/118% 対 87/80%、帯なしで測定）＝ §5-A の「順位合計が高いほど良い」と同じ現象を一般化したもの。
  得点変化・上がりなど本当に新しい特徴だけのモデルは弱い（帯なし・3期間で 🔥≥1: 94/109/132%、全組から最良1点: 91/92/97%）
- **弱いところ:** ①**確定オッズで測っている**（買う時点のオッズで同じになるかは不明。§5-H の前売りオッズが要る）
  ②特徴の組を8通り試した中の1つ ③モデルを作った2025年のほうが差が小さい（25Q4は逆）
- 使うには、買う時点の3連複オッズを**全組**読む必要がある（正規化に使う）。いまのアプリはオッズを読んでいない

分析のスクリプトは furoito の CSV を読むので、このリポジトリには入れていない（§2 のライセンスの件）。

## 5. 次にやること

### 保留中（データが貯まってから判断する）

| # | 内容 | 現状 | 判断できる時期 |
|---|---|---|---|
| A | **7車立ての順位合計を 12→13 に上げる** | 全期間で111.1%、1日5.5Rに減る | 2026-12頃 |
| B | **得点の序列が逆転したラインを外す** | n=34で的中2.9%。判断不能 | 2026-12頃 |

どちらも仕組みは用意済み（#9でオッズ、#10で競走得点が自動で貯まる）。
**3ヶ月後に「2026-09-22以降」だけで計算し直せば答えが出る。** 追加実装は不要。

**A の根拠**（順位合計の連続勾配。単一の閾値ではなく勾配なのが証拠）:
```
rs= 8: 72.6%   rs=11: 90.7%   rs=13: 108.5%
rs= 9: 72.7%   rs=12: 94.4%   rs=14: 114.9%
rs=10: 85.8%
```
的中率は8→12でほぼ横ばい（13.8〜14.5%）なのに平均配当が526→659円に上がる。
**同じオッズ帯の中で、順位合計が高いほど市場が長く付けるが当たる確率は落ちない。**

### 未着手

| # | 内容 | 備考 |
|---|---|---|
| C | 二車複・ワイドの検証 | **オッズを持っていないので測れない**。`odds.js` は `kakeshikiType=2shahuku`/`wide` に対応済みだが未取得。二車単は測って全滅（74〜84%） |
| D | `results.js` の前日ページ取得が無効 | 結果キーに日付8桁を含めた時点で一致しようがなくなっている。1日1リクエストの無駄 |
| E | `backfill.js` が Gamboo 依存 | WAFで閉鎖中。過去レースの穴埋めが必要になったら詰まる |
| F | **脚質データを history.json に保存する** | いまは出走表の生テキスト（`races.json` の git 履歴）か furoito から作るしかない。`riders` に逃・捲・差・マ・S・B・脚質表記を足せば、furoito が止まっても貯まる |
| G | **レースごとのS・B実績の収集** | 車番ごとのS取りやすさを直接測るのに要る。結果ページに記録があれば `odds.js` と同じく Actions で集める |
| H | ~~前売りオッズ（買う時点のオッズ）の記録~~ → **#23 で記録を開始**（`snap.js`。下の「締切前オッズの記録」） | 2〜3か月たまったら `snap_eval.js` で確定との差を測り、オッズで絞る改善を締切前オッズで測り直す |
| I | `simulate.js:408` の3単配当の絞り込みを直す | §2 の欠損の偏り。furoito で補うか、外れは0で数える |

---

## 6. 既知の落とし穴

### データ

- **`p3fpay` が三連複、`p3pay` は三連単**（§2）。最頻出の間違い。
- **`history.json` の日付は `YYYYMMDD`**。`buildDates` の引数だけが `YYYY-MM-DD`。
  `20260201` と入れると「日数指定」と解釈され 2,026万日になる。
- **`ranks` は印6つ分しかない。** 車立て・順位合計には使えない。`riders` を使う。
  `simulate.js` はこれで車立てを常に6として計算していた（#3で修正）。
- **競走得点は2026-08-28以降のみ**（`riders[6]`）。
- **`p3pay`（三連単）は荒れたレースほど欠けている**（§2）。`p3pay != null` で絞らない。
- **`results.js` は `races.json` に載っている日しか見ない。** 取り込みが遅れると前日を取り逃がす。
  `results.yml` の `gitfill.js`（直近3日）で埋めているので、この手順を外さないこと（§4-2 #18）。
- **`races.json` の git 履歴は 2026-07-10 から。** 出走表の生テキスト（脚質・決まり手・S・B）はそれより前に無い。
  セッションのクローンは浅い（途中までの履歴しか無い）ことがあるので、履歴を掘るときは `git fetch --unshallow` が要る。

### 履歴を消さないこと

- `backfill.js` の `slice(-20000)` と `results.js` の `slice(-8000)` が
  **過去5回、合計7,000件以上を消した**。どちらも削除済み。
  **履歴を書くコードは必ず「消さないか」を確認する。**
  `gitfill.js` / `scorelog.js` / `tokufill.js` には件数が減ったら中止する安全装置を入れてある。
- `races.json` を空配列で書いてアプリが「レースデータが空です」になった事故あり。
  `fetch.js` は0件なら書かずに `exit 1` する。
  アプリが期待する形は **`{updatedAt, count, races}`**（配列そのままはNG）。

### コード

- **engine.js と index.html は同じパーサを複製して持っている。** 片方だけ直すと表示と予想がズレる。
  2026-08-28の移行では `results.js` が取り残されて27日間の欠測になった。
- **`parseCard` の振り分け順は Gamboo → kdreams → WINTICKET。**
  `looksKdreams` は「`基本出走データ` が無い」かつ「`並び予想` がある」。
  緩めるとGambooのテキスト86件中35件が誤判定される。
- **kdreamsの並びの区切りは「中身が空の `<span class="icon_p space">`」。**
  タグを消す処理で真っ先に消えるので、`narabiFromHtml` で
  HTML→テキスト変換の**前**に `← 1 7 4・8 2・5 6 3` の1行に置き換える。
- **kdreamsはギヤが2つ並ぶ選手がいる**（ギヤ変更時）。位置決め打ちで
  `score = nums[1]` にすると2つ目のギヤを得点として読む。値域で見分ける
  （ギヤ 3.00〜4.50 / 得点 30〜121）。
- **定時実行(schedule)では `github.event.inputs.*` が全部空になる。**
  `odds.yml` はこれで無音の失敗をしていた。シェル側に既定値を入れること。
- **ガールズの隊形表示**: `1-1-1-1-1-1` のような全単騎は `patLabel()` が空文字を返す。
- **`isShoubu` に渡す車番**: `marksCars` は印6つ分。`rankedOfRow(x)` で `riders` から作る。
- **乱数**: ブートストラップで `s*1103515245` を使うと2^53を超えて壊れる。mulberry32を使う。
  ※mulberry32 はコードに入っていない。使い捨ての診断スクリプトに都度書く。

### 分析

- **過学習対策**: 条件は事前に宣言する／walk-forwardで検証する／**試した条件は全部報告する**。
  2026-09-21 に25候補を事前宣言して測り、9件が「前半・後半とも100%超」だったが、
  **そのほとんどは現行ルールの部分集合**で元の優位性を引き継いでいるだけだった。
- **「前半だけ良い」は過学習の典型。** 9車・単騎0人(115.4%→101.4%)、
  9車・オッズ5〜15倍(121.6%→101.7%)、本命が番手以降(122.4%→95.9%) はすべて却下。
- **部分集合は新発見ではない。** 順位合計だけは9段階の連続勾配という別種の証拠がある。
- 「レース番号中盤だけ回収率が良い」のような意味の分からない条件は、
  はむさんから明確に却下されている（実際は級班構成の代理変数だった）。正しい指摘。
- **結果を見てから見つけた組み合わせは、別の期間で確かめる。** 2026-09-25 に、76日のデータで見えた
  「相手に逃げ型1人→103%」「🔥×逃げ型→100% vs 80%」が、16か月では消えた。
- **閾値は評価スケールに依存する。** `rs≥12` は旧の重みで測ったもの。
  `weights.json` を変えると順位合計の分布がずれる（#8で rs=13 が692→647と7%減）。
  **採点を変えたら閾値も測り直すこと。**

---

## 7. 進め方のルール

- **推測で直さない。** 必ず実データ（HTML・ログ）を見てから直す。**診断を先に回す。**
- **まず結果だけ報告 → その後でファイルを作る**、という順番を好まれます。
- **数字は根拠（レース数・信頼区間）とセットで出す。**
- **間違えたら明示的に訂正する。**
- 良い数字が出たら、まず「なぜ出たか」を説明できるか確かめる。
  説明できない改善は採用しない。

---

## 8. 作業手順

### Claudeが作業するとき

1. セッション開始時に `honda1986/keirin` を **push権限** で追加してクローン
2. ブランチを切る（`claude/xxx`）
3. 直す → ローカルで検証（history.json と odds-*.json はローカルで使える）
4. commit → push → PR を作る
5. **マージは指示を仰ぐ**

### 実通信の検証

ローカルからは `keirin.kdreams.jp` に出られないことがある。その場合は
GitHub Actions の「評価値」(`diagnose.yml`) を該当ブランチで
`cmd: node xxx.js` として回す。読み取り専用なのでファイルは変わらない。

### アプリの表示確認

Chromium + Playwright でローカルサーバを立てて確認できる。
```
python3 -m http.server 8765    # リポジトリのルートで
```
開くと自動で今日のレースを読み込む。`#topList` / `#shoubuList` に `.rc`（🔥カード）が並ぶかを見る。
時刻で表示が変わる（あと○分／結果待ち）ので、Playwright の `page.clock.install({ time })` で時計を固定すると確かめやすい。

### ワークフロー一覧

| 名前 | ファイル | 起動 |
|---|---|---|
| 全レース更新 | `main.yml` | 1日4回（3:40/6:10/12:20/16:50 JST。定時実行が数時間遅れるので前倒ししてある） |
| 今日の結果 | `live.yml` | 起動したら10分おきに取り続け、5時間40分ごとに自分を起動し直す。08:07 JST＋予備3回で起動。深夜1〜8時は止まる。手動も可 |
| 結果収集 | `results.yml` | 1日1回（23:50 JST）。直近3日の漏れ埋め・成績の作り直しまで |
| オッズ取得 | `odds.yml` | 月1回（1日 11:00 JST）+ 手動 |
| 締切前オッズの記録（予備） | `snap.yml` | **ふだんは PC（`pc/`）が記録**。これは5分おきに PC の心拍（odds-snap の `pc/heartbeat.json`）を見て、25分以上古ければ代わりに記録する。5時間40分ごとに自分を起動し直して夜も止めずに続く。予備の定時実行 07:47/12:17/17:17 JST |
| シミュレーター | `Simulate.yml` | 手動（learn.js → simulate.js） |
| 履歴の穴埋め | `gitfill.yml` | 手動 |
| 評価値 | `diagnose.yml` | 手動（読み取り専用） |

---

## 9. 未解決・保留

- **移行後の実戦データが24日・147レースしかない。** 104.7%の再確認ができていない。
- **上がりタイムをどこから取るか。** Kドリームスは過去レース結果ページにしか無く取得コストが高い。
  移行時の実測では🔥判定の一致率に約4.7%効いていた。
- **2026-08-26・08-27 の2日は復旧不可。** Gamboo が WAF で落ちていて
  `races.json` 自体が無く、当日の予想が存在しない。約140レース。
- **競走得点は2026-08-28以降しか無い。** 得点の序列逆転（§5-B）を
  20ヶ月分で検証できない。（furoito には2022年からの競走得点があるので、結合すれば検証できる可能性がある）
- **自動投票は保留中。** `AUTOBET_SPEC.md` はオッズパーク向け。codegen の採取はあるが、
  締切時刻・合計金額・ログイン済みの目印などの採取が足りない（仕様書 5-4節）。
  **ログインID・パスワード・暗証番号はリポジトリに絶対に書かない。**

---

## 10. 一言で言うと

**動いている。数字も出ている。ただし「勝っている」と確定したわけではなく、2026年は落ちている。**

回収率104.7%（#8適用後106.3%）は出ているが、95%区間は100%をまたぎ、
利益の67%が1四半期に集中し、移行後のデータは24日分しかない。

🔥の3複1点は 2025年 103.1% → 2026年1〜4月 94.7% → 7〜9月 88.3%。
脚質は的中の予想には確かに効くが、市場も知っていて儲けにはほぼ直結しない。

次の判断（順位合計12→13、得点の序列逆転）は**データが貯まる2026年12月頃**。
それまでは仕組みを壊さず回し続けることが仕事。
